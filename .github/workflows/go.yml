# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Unitymetrics

on:
  push:
    branches: [ "master" ]
    paths-ignore:
      - '**.md'
  pull_request:
    branches: [ "master" ]
    paths-ignore:
      - '**.md'

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.13'
    - name: Check version Go
      run: |
          go version
    - name: Install dependencies Gounity
      run: |
          go get github.com/equelin/gounity
    - name: Install dependencies Logrus
      run: |  
          go get github.com/Sirupsen/logrus
          
    - name: Install
      run: go install 

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go build -v ./...
